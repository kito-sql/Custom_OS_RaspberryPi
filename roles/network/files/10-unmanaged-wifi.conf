[keyfile]
# wlan0 is managed by ifupdown + wpa_supplicant (via /etc/network/interfaces).
# Prevent NetworkManager from touching it to avoid conflicting with
# the wpa_cli-based wifi_connector.py provisioning tool.
unmanaged-devices=interface-name:wlan0
